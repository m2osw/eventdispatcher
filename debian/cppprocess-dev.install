# We have the eventdispatcher_qt.h in the same directory so to do it right
# we must list all of our includes, infortunately
usr/include/cppprocess/*.h
usr/lib/libcppprocess.so
usr/share/cmake/CppProcess/*
